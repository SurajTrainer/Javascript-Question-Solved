// create a const object in javascript can you change to hold a number later?   


const obj ={
    number :  34
}
console.log(obj);
obj.number = 23; 
console.log(obj);


// using chatgpt
// Creating a const object with a property holding a string
const myObject = {
    myProperty: "Hello"
  };
  
  // Displaying the original object
//   console.log(myObject);
  
  // Modifying the property value
  myObject.myProperty = 42;
  
  // Displaying the modified object
//   console.log(myObject);

// This would result in an error
// myObject = { newProperty: "New Value" };

  